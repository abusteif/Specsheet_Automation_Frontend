import React from "react";
import ReactDOM from "react-dom";

const LongFormItem = ({ children }) => {
  return <div className="long-line-form-item">{children}</div>;
};

export default LongFormItem;
